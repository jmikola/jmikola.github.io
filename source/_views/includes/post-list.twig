{% if posts is not empty %}
    <ul>
    {% for post in posts %}
        <li>
            <a href="{{ post.url }}">{{ post.title }}</a>
            <span class="post-date-inline">{{ post.date|date("M j, Y") }}</span>
        </li>
    {% endfor %}
    </ul>
{% else %}
{% endif %}
